# --- AI GENERATED FOR NOW ---
# This file documents config values used by the infrastructure and app.
# Copy to .env and fill in for local tooling if needed. CDK uses context (-c) and AWS env vars.

# CDK defaults to CDK_DEFAULT_ACCOUNT and CDK_DEFAULT_REGION from your AWS profile.
# You can override via: cdk deploy -c allowedOrigins='["http://localhost:3000"]'

# Optional: Secrets Manager ARN storing Spotify creds JSON
SPOTIFY_SECRET_ARN=arn:aws:secretsmanager:us-east-1:123456789012:secret:spotify-creds-abc

# Frontend origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,https://*.vercel.app
